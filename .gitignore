target
build
install
pom.xml
a.out
.nrepl-port
.jank-repl-history
storage.data
automation.rae

# screen-grab teardown markers (out-dir scratch; recover-pending! executes these)
docs/demos/.screen-grab-pending.edn*

# macOS
.DS_Store

# clj-kondo's analysis cache. The config beside it IS tracked - it is what
# makes clj-kondo usable on .jank sources at all.
.clj-kondo/.cache/
